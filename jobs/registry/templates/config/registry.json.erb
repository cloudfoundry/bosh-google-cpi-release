<%=
JSON.dump(
  "server" => {
    "protocol" => p("registry.protocol"),
    "address" => p("registry.listen_address"),
    "port" => p("registry.port"),
    "username" => p("registry.username"),
    "password" => p("registry.password"),
    "tls" => {
      "certfile" => "/var/vcap/jobs/registry/config/registry.certfile",
      "keyfile" => "/var/vcap/jobs/registry/config/registry.keyfile",
      "cacertfile" => "/var/vcap/jobs/registry/config/registry.cacertfile",
    }
  },
  "store" => {
    "adapter" => p("registry.store.adapter"),
    "options" => {
      "dbfile" => "/var/vcap/store/registry/registry.db"
    }
  }
)
%>
